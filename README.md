# summer_project


addded these files only 
git add embed.py fetch.py home.py rag_chain.py progress.txt

# add folders with useful code/templates
git add pages templates

dataset can fetched using the download link
